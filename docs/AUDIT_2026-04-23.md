# AUDIT — QBT Live 앱 전체 감사 보고서

> **작성일**: 2026-04-23 (KST)
> **재검증 / 사용자 결정 반영**: 2026-04-23 (KST)
> **대상 커밋**: `f06d8a3` (main)
> **범위**: `src/**/*.{ts,tsx}` 전체 (44개 파일) + 루트 문서(`CLAUDE.md`, `README.md`) + `docs/*.md` > **목적**: 코드/문서 전체에서 비즈니스 로직 오류, 버그, 상수화/리팩토링/구조 개선 기회, 불필요한 fallback, 문서/주석/코드 3자 불일치, 변경 가능성 높은 수치/리스트의 문서 직접 기재, 과거/이력/계획 단계 표기를 식별
> **본 문서는 분석 스냅샷이며, 후속 조치는 `docs/plans/PLAN_*.md` 로 진행한다.**

---

## 0. 요약 (재검증 후)

| #        | 카테고리                                | 1차 보고 | 재검증 후                      | 높음  | 중간   | 낮음  |
| -------- | --------------------------------------- | -------- | ------------------------------ | ----- | ------ | ----- |
| 1        | 비즈니스 로직 오류                      | 0        | 0                              | 0     | 0      | 0     |
| 2        | 버그                                    | 0        | 0                              | 0     | 0      | 0     |
| 3        | 상수화 필요                             | 4        | **2** (3-2, 3-4 폐기)          | 0     | 2      | 0     |
| 4        | 리팩토링                                | 5        | **4** (4-4 폐기)               | 0     | 3      | 1     |
| 5        | 구조개선                                | 0        | 0                              | —     | —      | —     |
| 6        | 불필요한 fallback                       | 4        | 4                              | 0     | 1      | 3     |
| 7        | 문서/주석/코드 3자 불일치               | 5        | 5                              | 1     | 3      | 1     |
| 8        | 문서 내 변경 가능 수치/리스트 직접 기재 | 5        | **4** (8-5 폐기 — 사용자 결정) | 0     | 2      | 2     |
| 9        | 과거/이력/계획 단계 표기                | 0        | 0                              | —     | —      | —     |
| **합계** |                                         | **23**   | **19**                         | **1** | **11** | **7** |

**전반 평가**: 코드 품질 양호. CLAUDE.md / DESIGN 규칙 대체로 잘 준수되고 있다. 치명적 런타임 결함이나 데이터 손상 위험은 발견되지 않았으며, 의존 방향(§19.3)/단일 store(§6.1)/유틸 순수성 등 구조 원칙도 위반 없음.

---

## 0-1. 재검증으로 폐기된 항목

| 번호    | 1차 보고 내용                            | 폐기 사유                                                                                                                                   |
| ------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| **3-2** | `withAlpha` 알파값 분산                  | 실제로는 `colors.ts` 내부에서만 사용. 외부 호출은 `Badge.tsx:11` 1곳뿐 — 분산 문제 없음                                                     |
| **3-4** | 폼 필터 라벨 추가 통합                   | `HistoryList.FILTERS` 로 이미 정리되어 있고, 추가 통합 가치 낮음                                                                            |
| **4-4** | AdjustForm 과도한 useMemo/useCallback    | 재검증 결과 `useMemo(payload→result)` 는 `validateBalanceAdjust` 호출 절감으로 의미 있음. `resetFields` 도 `onSubmit` deps 안정화 목적 정당 |
| **8-5** | Java Temurin 17 직접 기재 (CLAUDE.md §0) | 사용자 결정 (질문 2 — A): 그대로 유지 (개발 환경 전제조건은 명시 정당)                                                                      |

---

## 0-2. 사용자 결정 반영

| 질문                                        | 답변                                                               | 적용 항목     |
| ------------------------------------------- | ------------------------------------------------------------------ | ------------- |
| Q1: CLAUDE.md §0 라이브러리 11개 카테고리   | **C** (전부 제거 + `package.json` 참조 한 줄)                      | 7-5           |
| Q2: Java Temurin 17 표기                    | **A** (그대로 유지)                                                | 8-5 폐기      |
| Q3: `formatPendingShares` `close <= 0` 가드 | **C** (`__DEV__` throw + 빈 문자열 폴백)                           | 6-2           |
| Q4: 한글 메시지 상수화 범위                 | **A** (반복되는 "체결/보정/신호" + `directionLabel` 미사용 정리만) | 3-1           |
| Q5: 계획서 분배                             | **D** (4개)                                                        | §10 후속 조치 |

---

## 1. 비즈니스 로직 오류

(해당 없음)

**검토 결과**: 자산 ID 대소문자 규칙(저장 소문자/표시 대문자), KST 타임스탬프(`kstNow`), `drift_pct`/`weight` 비율 처리, USD 통화 단위, inbox `processed` 필드(앱 쓰기 금지/읽기 허용), 4자산 계산식 모두 설계서와 일치.

---

## 2. 버그

(해당 없음)

**검토 결과**: `useEffect` 의존성 배열 정확, WebView 메시지 화이트리스트 보호, `onAuthStateChanged` 단일 구독, 네트워크 판정식 §8.6 정책 일치, `firstDate` 빈 배열 분기는 명시적 보고 (§3.1 위반 아님).

---

## 3. 상수화 필요

### [3-1] "체결/보정/신호" 라벨 + `directionLabel` 미사용 정리 — **중간**

- **재집계 결과** (grep 검증):
  - `'체결'`: `TradeScreen.tsx:92`, `HistoryList.tsx:30, 82, 164` (4회)
  - `'보정'`: `TradeScreen.tsx:109`, `HistoryList.tsx:30, 83, 104, 168` (5회)
  - `'신호'`: `HistoryList.tsx:30, 84, 172` (3회)
  - `'전체'`: `HistoryList.tsx:28, 30, 82-84, 120` (6회 — `Filter` type 안)
  - `'매수'`/`'매도'` 직접 비교: `HistoryList.tsx:108-109` 1곳 (다른 곳은 `directionLabel()` 사용)
- 권장 조치:
  - `src/utils/constants.ts` 에 `EVENT_LABELS = { fill: '체결', balance_adjust: '보정', signal: '신호' } as const` 추가
  - `HistoryList.tsx:108-109` 의 직접 비교를 `directionLabel('buy'/'sell')` 호출로 교체
  - 단, `TradeScreen.tsx` 의 세그먼트 라벨은 `mode` 키('fill'/'adjust') 와 결합되어 있어 그대로 유지하거나 `EVENT_LABELS` 매핑 적용 (선택)
- 적용 PLAN: **PLAN_constants_messages**

### [3-3] `chartHtml.ts` 차트 색상 → `CHART_COLORS` 상수 — **중간**

- 위치: `src/utils/chartHtml.ts:15, 24-27, 51-54, 76, 79, 82, 85, 95-96`
- **검증된 매핑**:
  - `#161b22` (2회) = `COLORS.bg`
  - `#8b949e` (1회) = `COLORS.sub`
  - `#30363d` (3회, alpha `22` 변형 포함) = `COLORS.border`
  - `#58a6ff` (2회) = `COLORS.accent`
  - `#d29922` (1회) = `COLORS.yellow`
  - `#f85149` (3회, alpha `aa` 변형 포함) = `COLORS.red`
  - `#3fb950` (4회, alpha `aa` 변형 포함) = `COLORS.green`
- 현상: WebView 내부 HTML/CSS 컨텍스트로 RN `COLORS` 모듈 직접 import 불가. 그러나 hex 값을 템플릿 리터럴 보간으로 주입 가능.
- 권장 조치: `src/utils/colors.ts` (또는 `constants.ts`) 에 `CHART_COLORS` 객체 추가 → `generateChartHtml()` 가 보간으로 주입. 색상 SoT 단일화.
- 적용 PLAN: **PLAN_constants_messages**

---

## 4. 리팩토링

### [4-1] `ChartScreen.loadEarlierData` 의 price/equity 분기 중복 — **중간**

- 위치: `src/screens/ChartScreen.tsx:79-124` (46줄)
- 현상: `chartType === 'price'` / `else` 두 경로가 거의 동일 로직 (cache 추출 → firstDate 검사 → 연도 계산 → archive 로드).
- 권장 조치: `loadEarlierFor(target: 'price' | 'equity')` 헬퍼 추출 또는 store action 레벨에서 `loadArchive(target, year)` 단일 인터페이스 도입.
- 적용 PLAN: **PLAN_refactor_cleanup**

### [4-2] 폼 필드 에러 렌더 패턴 중복 — **중간**

- **검증된 카운트**: `FillForm.tsx` 5회 + `AdjustForm.tsx` 5회 = **총 10회 반복**
- 위치:
  - `FillForm.tsx:167-169, 216-218, 232-236, 249-253, 268-270`
  - `AdjustForm.tsx:149-151, 170-174, 187-191, 205-209, 239-243`
- 현상: `attempted && result.fieldErrors.X ? <Text style={styles.fieldError}>{...}</Text> : null` 패턴 동일 반복.
- 권장 조치: `src/components/FieldError.tsx` 컴포넌트 추출 — `<FieldError visible={attempted} message={result.fieldErrors.asset_id} />`.
- 적용 PLAN: **PLAN_form_field_error** (단독 PLAN)

### [4-3] `HistoryList` 이벤트 타입별 렌더 분기 — **중간**

- 위치: `src/components/HistoryList.tsx:157-188`
- 현상: `if (e.kind === 'fill') / else if 'balance_adjust' / else` 삼단 분기. `HistoryEvent` discriminated union 의 `never` 검사 없음 → 새 `kind` 추가 시 컴파일 보호 없음.
- 권장 조치: 마지막 `else` 블록 끝에 `const _exhaustive: never = e; throw new Error(...)` 추가 (CLAUDE.md §3.1 방식 2).
- 적용 PLAN: **PLAN_refactor_cleanup**

### [4-5] `useStore.ts` 섹션 정리 — **낮음 (주석만)**

- 위치: `src/store/useStore.ts` 전체
- 현상: 단일 store 원칙(§6.1) 준수로 자연스럽게 비대. 섹션 주석으로 가독성 향상 여지.
- 권장 조치: 동작 변경 없이 섹션 주석(`// ─── refresh actions ───`, `// ─── inbox writes ───` 등) 추가.
- 적용 PLAN: **PLAN_refactor_cleanup**

---

## 5. 구조개선

(해당 없음 — 위반 사례 없음)

**검토 결과**: 의존 방향, 컴포넌트 분류, 서비스/유틸 순수성, 타입 중앙화, 네비게이션 책임 분리 모두 준수.

---

## 6. 불필요한 fallback

### [6-1] `services/chartInject.ts` 의 webview null 체크 — **중간**

- 위치: `src/services/chartInject.ts:16, 31`
- 현상: 호출부(`ChartScreen.injectChartData`) 가 `webviewReady` 플래그로 이미 보호. `webviewRef.current` null 도달 불가능.
- 권장 조치: `__DEV__` 가드 + throw (§3.1 방식 1) — 개발 빌드에서 즉시 인지, 릴리스는 안전 폴백.
- 적용 PLAN: **PLAN_refactor_cleanup**

### [6-2] `formatPendingShares` 의 `close <= 0` 가드 — **낮음**

- 위치: `src/utils/format.ts:135-141`
- 사용자 결정 (Q3 — C): `__DEV__` throw + 빈 문자열 폴백 적용
- 권장 조치:
  ```ts
  if (close == null || close <= 0) {
    if (__DEV__) {
      throw new Error(
        `[format] 내부 불변조건 위반: formatPendingShares 의 close=${close} (양수 종가 기대)`,
      );
    }
    console.error(
      '[format] 내부 불변조건 위반: formatPendingShares close=',
      close,
    );
    return '';
  }
  ```
- 적용 PLAN: **PLAN_refactor_cleanup**

### [6-3] `PendingOrdersListBlock` 의 `data as { asset_id?: string }` 단언 — **낮음**

- 위치: `src/components/PendingOrdersListBlock.tsx:35-37`
- 현상: ad-hoc 타입 단언으로 `InboxItem` 의 `data` 필드 접근.
- 권장 조치: `src/types/rtdb.ts` 의 `InboxItem` 을 제네릭(`InboxItem<T>`) 또는 discriminated union 으로 정의하여 단언 제거.
- 적용 PLAN: **PLAN_refactor_cleanup**

### [6-4] `validation.ts` 의 trade_date/필드 부재 분기 — **낮음**

- 위치: `src/utils/validation.ts:46, 85-86`
- 권장 조치: **현 상태 유지** (외부 입력 검증 깊은 방어는 §11.4 옳음).
- 적용 PLAN: 변경 없음

---

## 7. 문서/주석/코드 3자 불일치

### [7-1] CLAUDE.md §15.3 의 상수 목록이 실제와 불일치 — **높음**

- 위치: `CLAUDE.md:803` (§15.3 환경변수)
- 현상: 문서에 13개 상수만 나열. 실제 [src/utils/constants.ts](../src/utils/constants.ts) 에는 추가로 `MS_PER_DAY`, `RADIUS_MD`, `PADDING_SM`, `PADDING_MD`, `MARGIN_SM`, `MARGIN_MD`, `TOAST_MESSAGES` 가 존재 (총 20개).
- 문제: 동시에 §7.6 (문서 내구성 원칙) 위반.
- 권장 조치: §15.3 에서 상수 목록 직접 나열을 삭제하고 "전체 목록은 [`src/utils/constants.ts`](../src/utils/constants.ts) 참조" 한 줄로 대체.
- 적용 PLAN: **PLAN_docs_alignment**

### [7-2] `docs/COMMANDS.md` 의 `npm test` 가 `package.json` 에 부재 — **중간**

- 위치: [docs/COMMANDS.md:42](COMMANDS.md#L42) — `npm test  # Jest`
- 현상: `package.json::scripts` 에 `test` 항목 없음. CLAUDE.md §16 ("단위 테스트 파일 없음") 와 모순.
- 권장 조치: COMMANDS.md 에서 `npm test` 라인 삭제.
- 적용 PLAN: **PLAN_docs_alignment**

### [7-3] `chartHtml.ts` 헤더 주석의 잘못된 § 번호 인용 — **중간**

- 위치: [src/utils/chartHtml.ts:1-4](../src/utils/chartHtml.ts#L1-L4)
- 현상: `"CLAUDE.md §7 WebView 범주 — §3.3 RN 컴포넌트 하드코딩 금지 규칙의 예외"`. 실제 §7 = "UI 텍스트 / 네이밍 규칙", §3.3 미존재. 색상 하드코딩 규칙은 §5.3 / §5.4.
- 권장 조치: `"CLAUDE.md §5.3 / §5.4 (스타일링 — 하드코딩 색상 hex 금지) 의 예외"` 로 수정.
- 적용 PLAN: **PLAN_docs_alignment** (또는 PLAN_constants_messages 의 CHART_COLORS 도입과 동시 처리)

### [7-4] `README.md` 폴더 구조의 파일 목록 불완전 — **중간**

- 위치: [README.md:46-50](../README.md#L46-L50)
- 현상:
  - line 47 (components): `Badge, FillForm, ChartWebView, OfflineScreen 등` — 실제 22개
  - line 48 (services): `firebase, auth, rtdb, fcm, network, chart` — `chartInject` 누락 (실제 7개)
  - line 50 (utils): `format, validation, colors, constants, chartHtml` — `loadingKeys`, `parse`, `pressable`, `useDatePicker` 누락 (실제 9개)
- 권장 조치: 파일명 직접 나열 제거. `# 외부 I/O — 자세한 목록은 src/services/ 참조` 형태.
- 적용 PLAN: **PLAN_docs_alignment**

### [7-5] CLAUDE.md §0 프로젝트 개요의 라이브러리 나열 — **낮음** (사용자 결정 Q1: C)

- 위치: [CLAUDE.md:33-43](../CLAUDE.md#L33-L43) ("주요 라이브러리")
- 사용자 결정: 11개 카테고리 전부 제거하고 `"주요 라이브러리는 \`package.json\` 참조"` 한 줄로 대체.
- 적용 PLAN: **PLAN_docs_alignment**

---

## 8. 문서 내 "변경 가능성 높은 수치/리스트" 직접 기재

### [8-1] CLAUDE.md §15.1 의 `applicationId` 직접 기재 — **중간**

- 위치: [CLAUDE.md:790](../CLAUDE.md#L790)
- 현상: `` - `applicationId` / `namespace` 는 `android/app/build.gradle` 참조 (`com.ingbeen.qbtlive`) ``
- 권장 조치: 괄호 부분 `(com.ingbeen.qbtlive)` 삭제. 참조 문구만 유지.
- 적용 PLAN: **PLAN_docs_alignment**

### [8-2] DESIGN_QBT_LIVE_FINAL.md `recent_months` 의 값 노출 — **중간**

- 위치: [docs/DESIGN_QBT_LIVE_FINAL.md:463](DESIGN_QBT_LIVE_FINAL.md#L463) (`recent_months` 행) + line 554 (사용자 IDE 선택 위치)
- 현상: 표 셀에 ``상수 `CHART_RECENT_MONTHS = 6``` 로 값까지 기재.
- 권장 조치: `` 상수 `CHART_RECENT_MONTHS` 참조 `` 로 수정 (값 `= 6` 제거). 같은 패턴이 다른 곳에 있다면 동일 적용.
- 적용 PLAN: **PLAN_docs_alignment**

### [8-3] CLAUDE.md §7.5 의 RN 버전 예시 — **낮음**

- 위치: [CLAUDE.md:452](../CLAUDE.md#L452)
- 현상: `"RN 0.85 에서 New Architecture 강제, 끌 수 없음"`.
- 권장 조치: `"RN 0.82+ 에서 New Architecture 강제..."` 로 일반화.
- 적용 PLAN: **PLAN_docs_alignment**

### [8-4] DESIGN_QBT_LIVE_FINAL.md 예제 JSON 의 구체적 날짜 — **낮음**

- 위치: [docs/DESIGN_QBT_LIVE_FINAL.md:447-448](DESIGN_QBT_LIVE_FINAL.md#L447-L448)
- 현상: 예제 JSON 의 `"first_date": "2013-01-02"`, `"last_date": "2026-04-14"` 등 구체적 날짜.
- 권장 조치: 예제 날짜 옆에 의미 주석 추가 (`"2013-01-02"  // 자산 상장일 예시`) 또는 일반 placeholder 로 변경. 단, 예제는 사람이 보기 좋아야 하므로 완전한 placeholder 보다 "대표값 + 의미 주석" 권장.
- 적용 PLAN: **PLAN_docs_alignment**

---

## 9. 문서 및 주석에 과거 상태/변경 이력/계획 단계 기재

(해당 없음)

---

## 10. 후속 조치 — 계획서 분배 (사용자 결정 Q5: D)

| PLAN                        | 포함 항목                                   | 예상 변경 파일                                                                                                                                                                                                   | 우선순위 |
| --------------------------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| **PLAN_docs_alignment**     | 7-1, 7-2, 7-3, 7-4, 7-5, 8-1, 8-2, 8-3, 8-4 | `CLAUDE.md`, `README.md`, `docs/COMMANDS.md`, `docs/DESIGN_QBT_LIVE_FINAL.md`, `src/utils/chartHtml.ts` (헤더 주석만)                                                                                            | 1        |
| **PLAN_constants_messages** | 3-1, 3-3                                    | `src/utils/constants.ts` (또는 `colors.ts`), `src/utils/chartHtml.ts`, `src/components/HistoryList.tsx`, `src/screens/TradeScreen.tsx`                                                                           | 2        |
| **PLAN_form_field_error**   | 4-2                                         | 신규 `src/components/FieldError.tsx`, `src/components/FillForm.tsx`, `src/components/AdjustForm.tsx`                                                                                                             | 3        |
| **PLAN_refactor_cleanup**   | 4-1, 4-3, 4-5, 6-1, 6-2, 6-3                | `src/screens/ChartScreen.tsx`, `src/components/HistoryList.tsx`, `src/store/useStore.ts`, `src/services/chartInject.ts`, `src/utils/format.ts`, `src/components/PendingOrdersListBlock.tsx`, `src/types/rtdb.ts` | 4        |

---

## 11. 분석 방법 및 한계

- **분석 대상**: `src/**/*.{ts,tsx}` 44 개 + 루트 `CLAUDE.md` / `README.md` + `docs/` 4 개 문서 + `package.json` + `android/app/build.gradle`.
- **방법**: 3개 Explore 서브에이전트 병렬 분석 → 메인 에이전트 교차검증 → 사용자 추가 결정 반영.
- **검증 결과 폐기된 false positive**:
  - "APP_VERSION 동기화 어긋남" → 3곳 모두 `'1.0.0'` 일치 확인.
  - "drift_pct 포맷 불일치" → `formatDriftPct` 가 설계서 §12 정의와 일치.
  - "ChartScreen useEffect deps 누락" → 모두 정확히 명시됨.
  - "네트워크 판정식 오류" → §8.6 정책과 일치.
  - "withAlpha 알파값 분산" → `colors.ts` 내부 + `Badge.tsx` 1곳뿐.
  - "AdjustForm 과도한 useMemo/useCallback" → `validateBalanceAdjust` 호출 절감으로 정당.
- **한계**: 런타임 동작 검증(에뮬레이터 실기) 미포함. 실기 검증은 사용자 책임.

---

**문서 끝**
