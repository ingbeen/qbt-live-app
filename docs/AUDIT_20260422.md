# AUDIT 2026-04-22 — 전체 재감사 리스트업

> **목적**: `qbt-live-app` 전체 소스/문서를 9개 카테고리로 감사하여 발견 항목을 단일 SoT 로 리스트업.
> **선행 기준**: 최근 "감사 사이클 종결"(`349b697`) 직후 상태. 그 이후 누락/신규 이슈 포함한 전체 재감사.
> **상태**: 리스트업만 완료. **수정은 사용자 승인 후 항목별로 진행** (Phase 단위 커밋 원칙 준수).
> **판정 기준**: `CLAUDE.md` (§5.5 주석 원칙, §17 개발 원칙 등), `docs/DESIGN_QBT_LIVE_FINAL.md` (서버 SoT).

## 감사 범위

- 대상: `src/**/*.{ts,tsx}` 40 파일 / 5273 줄
- 문서: `CLAUDE.md`, `README.md`, `docs/DESIGN_QBT_LIVE_FINAL.md`, `docs/COMMANDS.md`
- 빌드 설정: `package.json`, `android/app/build.gradle`
- 방법: 3개 Explore 서브에이전트 병렬 투입 (로직/품질/문서 정합성 분담)

---

## 1. 비즈니스 로직 오류

### [BL-01] `drift_pct` 필드는 타입에 있으나 UI 미표시 — **완료 (PLAN_01)**
- 파일: `src/types/rtdb.ts` (정의) / 사용처 없음
- 현재: 서버가 `/latest/portfolio.drift_pct` 를 항상 제공하지만 앱 어디에서도 표시하지 않음
- 문제: 설계서상 포트폴리오 전체 drift 는 핵심 지표인데, 사용자는 Model/Actual 카드의 수치 차이로 간접 추론만 가능
- 근거: `docs/DESIGN_QBT_LIVE_FINAL.md §8.2.1`, §11
- 우선순위: Medium (사용자 의도 확인 필요 — 의도적으로 감춘 것인지)
- **처리 결과** (서버 측 정책 확정 후):
  - `Portfolio.drift_pct` 스칼라 → `ModelCompareCard` 헤더 배지 + 토글 본문에 값만 표시 (상태 라벨 없음)
  - `EquityChartSeries.drift_pct` 배열 → 서버가 제거, 앱 타입/병합 코드 전면 제거
  - 참고: [docs/PLAN_01_drift_pct.md](PLAN_01_drift_pct.md)

---

## 2. 버그

### [BUG-01] `formatPendingShares` 가 `close<=0` 일 때 빈 문자열 반환 → UI 손상 — **완료 (PLAN_02)**
- 파일: [src/utils/format.ts:130-136](src/utils/format.ts#L130-L136)
- 호출부: [src/components/ReminderBlock.tsx:49-52](src/components/ReminderBlock.tsx#L49-L52), [src/components/SignalNextFillBlock.tsx:32-34](src/components/SignalNextFillBlock.tsx#L32-L34)
- 현재: `if (close == null || close <= 0) return '';` → `"SSO  매수 pending"` 처럼 주식수 자리가 비어보임
- 문제: 설계상 `close` 는 항상 양수. 호출부에서 signals 존재 확인 없이 호출하는 것이 원인
- 개선 방향: 호출부에서 signals 로딩 확인 후 호출, 또는 대시(`-`) 표시로 폴백 변경
- 우선순위: Medium (UI 가독성)
- **처리 결과**: `HomeScreen` 에서 `signals` null 체크 후 `ReminderBlock` / `SignalNextFillBlock` 렌더하도록 변경. 두 컴포넌트 signals prop non-null 타입으로 확정 → 옵셔널 체이닝 제거.

### [BUG-02] `ChartScreen` 빈 `dates` 배열 처리는 방어되나 가독성 낮음 — **완료 (PLAN_02)**
- 파일: [src/screens/ChartScreen.tsx:94-98](src/screens/ChartScreen.tsx#L94-L98), [src/screens/ChartScreen.tsx:114-118](src/screens/ChartScreen.tsx#L114-L118)
- 현재: `parseInt((recent.dates[0] ?? '').slice(0, 4), 10)` → `NaN` 발생 가능 → `!Number.isFinite()` 체크로 방어
- 문제: 방어 되나 의도가 불명확. `dates.length === 0` 먼저 체크하는 가드가 명확
- 우선순위: Low (현재 안전)
- **처리 결과**: `firstDate = recent.dates[0]` 변수로 받아 `if (!firstDate) return` 단일 가드로 정리. 가독성 개선.

### [BUG-03] `AdjustForm` 의 `currentShares` 타입 좁히기 불명확 — **완료 (PLAN_02)**
- 파일: [src/components/AdjustForm.tsx:122-125](src/components/AdjustForm.tsx#L122-L125), [src/components/AdjustForm.tsx:166](src/components/AdjustForm.tsx#L166)
- 현재: `isAsset` 일 때만 `number`, 아니면 `null` → 사용 시 `formatShares(currentShares ?? 0)`
- 문제: TypeScript 가 `isAsset` 를 좁혀주지 못해 `?? 0` 필수. 로직상 불필요한 fallback (FB-02 와 중복)
- 우선순위: Low
- **처리 결과**: `isAssetTarget(t): t is AssetId` 타입 가드 함수 도입. `currentShares` 변수 제거하고 블록 내부에서 `portfolio.assets[target].actual_shares` 직접 접근.

---

## 3. 상수화 필요

### [CONST-01] 공통 `borderRadius: 8` 10회+ 반복 — **완료 (PLAN_03)**
- 파일: [src/components/Toast.tsx:50](src/components/Toast.tsx#L50), [src/screens/LoginScreen.tsx:118](src/screens/LoginScreen.tsx#L118), [src/screens/LoginScreen.tsx:134](src/screens/LoginScreen.tsx#L134), [src/components/ChartTypeToggle.tsx:57](src/components/ChartTypeToggle.tsx#L57), [src/components/MAProximityCard.tsx:38](src/components/MAProximityCard.tsx#L38), [src/components/SyncDialog.tsx:165](src/components/SyncDialog.tsx#L165), [src/components/ErrorState.tsx:42](src/components/ErrorState.tsx#L42) 외 다수
- 중복 대상: `borderRadius: 8`
- 제안 상수명: `RADIUS_MD` 또는 `BORDER_RADIUS_STANDARD`
- 위치 제안: `src/utils/constants.ts` 또는 신규 `src/utils/layout.ts`
- 우선순위: Medium

### [CONST-02] 공통 카드 패딩 (`12`, `14`) 반복 — **완료 (PLAN_03)**
- 파일: [src/components/Toast.tsx:51](src/components/Toast.tsx#L51), [src/components/ReminderBlock.tsx:70](src/components/ReminderBlock.tsx#L70), [src/components/SignalNextFillBlock.tsx:53](src/components/SignalNextFillBlock.tsx#L53), [src/components/MAProximityCard.tsx:39](src/components/MAProximityCard.tsx#L39), [src/components/SyncDialog.tsx:150](src/components/SyncDialog.tsx#L150), [src/components/AssetSummaryCard.tsx:135](src/components/AssetSummaryCard.tsx#L135), [src/components/ModelCompareCard.tsx:157](src/components/ModelCompareCard.tsx#L157)
- 중복 대상: `padding: 12` (5회), `padding: 14` (3회)
- 제안 상수명: `PADDING_CARD_SM`, `PADDING_CARD_MD`
- 우선순위: Medium

### [CONST-03] 공통 마진 (`marginBottom: 12`, `8`) 반복 — **완료 (PLAN_03)**
- 파일: [src/components/Toast.tsx:44](src/components/Toast.tsx#L44), [src/components/ReminderBlock.tsx:71](src/components/ReminderBlock.tsx#L71), [src/components/SignalNextFillBlock.tsx:54](src/components/SignalNextFillBlock.tsx#L54), [src/components/MAProximityCard.tsx:40](src/components/MAProximityCard.tsx#L40), [src/screens/LoginScreen.tsx:112](src/screens/LoginScreen.tsx#L112)
- 중복 대상: `marginBottom: 12` / `marginBottom: 8`
- 제안 상수명: `MARGIN_MD`, `MARGIN_SM`
- 우선순위: Medium

### [CONST-04] 토스트 메시지 템플릿 반복 — **완료 (PLAN_04)**
- 파일: [src/store/useStore.ts:243-244](src/store/useStore.ts#L243-L244), [src/store/useStore.ts:281](src/store/useStore.ts#L281), [src/store/useStore.ts:295](src/store/useStore.ts#L295), [src/store/useStore.ts:310](src/store/useStore.ts#L310)
- 중복 대상: `"저장되었습니다.\n다음 실행에 반영됩니다."` 형태 메시지 4회
- 제안: `src/utils/constants.ts` 에 `TOAST_MESSAGES = { modelSync, fill, adjust, fillDismiss }` 객체
- 우선순위: Low (의미 단위 분리 여부는 검토 필요)

---

## 4. 리팩토링

### [RF-01] `ReminderBlock` ↔ `SignalNextFillBlock` 거의 동일 — **완료 (PLAN_04)**
- 파일: [src/components/ReminderBlock.tsx](src/components/ReminderBlock.tsx), [src/components/SignalNextFillBlock.tsx](src/components/SignalNextFillBlock.tsx)
- 현재: 두 컴포넌트가 동일 렌더링 패턴, 스타일 거의 일치, 필터 조건만 다름
- 근거: CLAUDE.md §17.2 간결성, §17.3 확장성
- 제안: 공통 컴포넌트로 추출, `mode` prop 으로 필터 분기
- 우선순위: Medium

### [RF-02] 세그먼트 토글 UI 중복 — **보류 (YAGNI)**
- 파일: [src/screens/TradeScreen.tsx:75-110](src/screens/TradeScreen.tsx#L75-L110), [src/components/ChartTypeToggle.tsx](src/components/ChartTypeToggle.tsx)
- 현재: `fill/adjust` 토글과 `price/equity` 토글이 동일 UI 패턴을 각각 재구현
- 제안: `SegmentControl` 공통 컴포넌트 (`value`, `options`, `onChange`)
- 우선순위: Medium

### [RF-03] `ChartScreen` 내부 로직 비대 — **완료 (PLAN_04)**
- 파일: [src/screens/ChartScreen.tsx](src/screens/ChartScreen.tsx)
- 현재: 상태 관리(`chartType`, `assetId`, `webviewReady`) + 3개 Effect + 콜백 + 60줄 이상의 `injectChartData` 가 한 파일에 혼재
- 제안: `useChartInjection()` 커스텀 훅 또는 `src/services/chartInjection.ts` 로 주입 로직 분리
- 우선순위: Medium

### [RF-04] `FillForm` / `AdjustForm` 대형 파일 — **완료 (PLAN_04)**
- 파일: [src/components/FillForm.tsx](src/components/FillForm.tsx) (475줄), [src/components/AdjustForm.tsx](src/components/AdjustForm.tsx) (382줄)
- 현재: 상태 + 검증 + DatePicker + 제출 로직 일체형
- 제안: DatePicker 핸들링을 `useDatePicker()` 훅으로 공통 추출
- 우선순위: Low (기능 복잡도상 현 구조 허용 범위이나, 두 폼 공통 훅은 이득)

### [RF-05] `Pressable` + `pressed && opacity` 패턴 반복 — **완료 (PLAN_03)**

---
- 파일: [src/screens/LoginScreen.tsx:74-78](src/screens/LoginScreen.tsx#L74-L78), [src/screens/TradeScreen.tsx:76-91](src/screens/TradeScreen.tsx#L76-L91), [src/components/SyncDialog.tsx:69-89](src/components/SyncDialog.tsx#L69-L89), [src/components/ModelCompareCard.tsx:29-34](src/components/ModelCompareCard.tsx#L29-L34) 외 다수
- 현재: `pressed && { opacity: 0.7 }` 10회 이상 반복
- 제안: 스타일 헬퍼 `pressedOpacity(pressed)` 또는 `PressableOpacity` 래퍼
- 우선순위: Low

---

## 5. 구조개선

### [ST-01] services 가 store 를 import (의존 방향 위반) — **완료 (PLAN_04)**
- 파일: [src/services/auth.ts:7](src/services/auth.ts#L7), [src/services/fcm.ts:14](src/services/fcm.ts#L14), [src/services/network.ts:2](src/services/network.ts#L2)
- 현재: `services` 계층이 `useStore.getState()` 직접 호출 → 상태 동기화와 I/O 결합
- 근거: CLAUDE.md §17.3 의존 방향 `screens → components → services → utils`, §8.1 "services 는 순수 I/O"
- 제안:
  1. `auth.ts::signOut` → store 업데이트 제거, 호출부(App.tsx/LoginScreen) 에서 `clearAll() + setUser(null)`
  2. `fcm.ts` → `setFcmRegistered` 호출 제거, App.tsx 에서 담당
  3. `network.ts` → `setOnline` 호출 제거, App.tsx 의 setupNetworkListener 래퍼에서 처리
- 우선순위: High (아키텍처 정책 위반, 재사용성 저하)

### [ST-02] 타입 정의 분산 — **완료 (PLAN_04)**
- 파일: [src/services/rtdb.ts](src/services/rtdb.ts) 내 `InboxItem`, `SignalHistoryEntry` 등
- 현재: RTDB 타입이 `src/types/rtdb.ts` 외부에 일부 정의됨
- 근거: CLAUDE.md §8.1 "`types/` 는 타입 정의만"
- 제안: RTDB 관련 타입 전체를 `src/types/rtdb.ts` 로 이동
- 우선순위: Low

### [ST-03] 차트 loading key 문자열 조합 분산 — **완료 (PLAN_04)**
- 파일: [src/store/useStore.ts:322](src/store/useStore.ts#L322), [src/store/useStore.ts:368](src/store/useStore.ts#L368), [src/store/useStore.ts:398](src/store/useStore.ts#L398)
- 현재: `chart_${target}`, `chart_archive_${assetId}_${year}`, `chart_archive_equity_${year}` 를 여러 곳에서 직접 조합
- 제안: `makeChartLoadingKey()` 등 헬퍼로 격리 (오타 방지 + 단일 변경점)
- 우선순위: Low

### [ST-04] `ModelCompareCard` ↔ `AssetSummaryCard` 구조 유사 — **보류 (YAGNI)**
- 파일: [src/components/ModelCompareCard.tsx](src/components/ModelCompareCard.tsx), [src/components/AssetSummaryCard.tsx](src/components/AssetSummaryCard.tsx)
- 현재: 둘 다 `ASSETS.map()` 패턴, 카드 → divider → rows 스타일 동일
- 제안: row 렌더링만이라도 공통화, 또는 일반화 추상 섣불리 금지 (YAGNI)
- 우선순위: Low (현재 YAGNI 허용 범위)

---

## 6. 불필요한 fallback

### [FB-01] `MAProximityCard` 에서 `ma_distance_pct ?? 0` — **완료 (PLAN_02)**
- 파일: [src/components/MAProximityCard.tsx:18](src/components/MAProximityCard.tsx#L18)
- 현재: `signals?.[id]?.ma_distance_pct ?? 0`
- 문제: 설계상 signals 로딩 완료 시 4자산 모두 포함됨. 로딩 중 렌더는 부모에서 차단해야 함
- 제안: 부모에서 `signals` null 가드 후 렌더, 컴포넌트 내부 fallback 제거
- 우선순위: Low
- **처리 결과**: `HomeScreen` 에서 `signals` null 차단 후 렌더. `signals` prop non-null 타입 확정, 내부 옵셔널/fallback 제거.

### [FB-02] `AdjustForm` 의 `currentShares ?? 0` 과잉 방어 — **완료 (PLAN_02)**
- 파일: [src/components/AdjustForm.tsx:166](src/components/AdjustForm.tsx#L166)
- 현재: `{formatShares(currentShares ?? 0)}`
- 문제: `isAsset` true 일 때 `currentShares` 는 항상 `number` (설계서 §8.2.1)
- 제안: `isAsset` 분기로 타입 좁히거나 컴포넌트를 자산용/현금용으로 분리
- 우선순위: Low
- **처리 결과**: BUG-03 과 동일 처리 — `isAssetTarget` 타입 가드 도입으로 타입 좁힘.

### [FB-03] `validation.ts` 의 `owned ?? 0` — **완료 (PLAN_02)**
- 파일: [src/utils/validation.ts:59](src/utils/validation.ts#L59), [src/utils/validation.ts:127](src/utils/validation.ts#L127)
- 현재: `portfolio.assets[p.asset_id]?.actual_shares ?? 0`
- 문제: 설계서 §8.2.1 에서 자산 키 4개 항상 존재 보장 → `?.` 와 `?? 0` 모두 불필요
- 제안: `portfolio.assets[p.asset_id as AssetId].actual_shares` 직접 접근 + 자산 키 유효성은 `asset_id` 검증 단계에서 처리
- 우선순위: Low
- **처리 결과**: 2곳 모두 옵셔널 / `?? 0` / `as AssetId` 캐스트 제거. `AssetId` import 도 불필요해져 함께 제거.

---

## 7. 문서/주석/코드 3자 불일치

### [MM-01] 자산 ID 목록 문서 내 반복 기재
- 위치 A: `docs/DESIGN_QBT_LIVE_FINAL.md` — `sso`, `qld`, `gld`, `tlt` 문장 본문에 21회+ 반복
- 위치 B: [src/utils/constants.ts:59](src/utils/constants.ts#L59) — `ASSETS` 상수 (SoT)
- 어긋남: DESIGN 문서가 상수 링크만으로 충분한 곳에도 자산 리스트 본문 반복 → 자산 확장 시 문서 갱신 누락 위험
- 우선순위: Low (현재 정확하나 유지보수 부담)

> **참고 — 정상 확인된 항목**
> - `APP_VERSION` 3곳 동기화 (`1.0.0`): [src/utils/constants.ts:10](src/utils/constants.ts#L10), `package.json:3`, `android/app/build.gradle:87` 모두 일치
> - `CLAUDE.md §16.2` 명령어 단일 관리처 원칙: `README.md` 는 `docs/COMMANDS.md` 링크만 참조, 준수
> - src 파일 주석 전수 검사 결과 §5.5 금지 패턴 발견 없음

---

## 8. 문서 내 쉽게 변경될 수치/리스트

### [DV-01] DESIGN 문서의 MVP / "향후 추가" 계획 표현 — **완료 (PLAN_04)**
- 문서: `docs/DESIGN_QBT_LIVE_FINAL.md §1.3:104`
- 하드코딩 값: `"현재 MVP 범위 밖"`, `"향후 추가 시 자산별 currency / market 필드 도입..."`
- 실제 SoT: [src/utils/constants.ts::ASSETS](src/utils/constants.ts#L59) (현재 4자산 고정)
- 제안: 로드맵 분리 또는 DESIGN 문서 내 "ROADMAP" 섹션으로 이동. 설계 자체는 현재 상태만 서술
- 우선순위: Medium

### [DV-02] `CHART_LIB_VERSION` 설계 문서 본문 반복 가능성 점검 대상
- 문서: `CLAUDE.md §7.4` — "버전은 `CHART_LIB_VERSION` 참조" (링크만, 준수)
- 실제 SoT: [src/utils/constants.ts::CHART_LIB_VERSION](src/utils/constants.ts) (SoT)
- 어긋남: 없음 (정상 사례로 기록)
- 우선순위: N/A

---

## 9. 문서·주석 내 과거 상태/변경 이력/계획 단계

### [HIST-01] DESIGN 문서의 "제거되었다" 과거 상태 기술 — **자동 해결**
- 위치: `docs/DESIGN_QBT_LIVE_FINAL.md §8.2.4:431`
- 표현: `"이 경로는 제거되었다. 과거에는 drift_pct / model_equity / actual_equity 세 필드를 별도 경로로 노출했으나..."`, `"완전한 중복이었고"`
- 근거: CLAUDE.md §5.5 "과거 상태 기록 금지", "변경 이력 기록 금지"
- 제안: 현재 상태(`"drift_pct 는 /latest/portfolio 에서만 읽는다"`)만 남기고 삭제된 경로 설명 제거
- 우선순위: High (명시적 금지 패턴 위반)
- **처리 결과**: 서버 측에서 `§8.2.4` 제목을 `"drift 스칼라 — /latest/portfolio 에 통합"` 으로 정리하고 과거 상태 서술 전면 제거. 앱 별도 조치 불필요.

### [HIST-02] DESIGN 문서의 "향후 추가" 미래 계획 — **완료 (PLAN_04)**
- 위치: `docs/DESIGN_QBT_LIVE_FINAL.md §1.3:104`
- 표현: `"향후 추가 시 자산별 currency / market 필드 도입..."`
- 근거: CLAUDE.md §5.5 "계획 단계 표현 금지"
- 제안: 로드맵 문서 분리 또는 DESIGN 문서 맨 끝 "ROADMAP" 별도 섹션으로 격리
- 우선순위: Medium

### [HIST-03] DESIGN 문서의 "이전 연도" 시간 의존적 표현
- 위치: `docs/DESIGN_QBT_LIVE_FINAL.md §5:196`, `§8.2.5:443`, `§8.2.6:539`
- 표현: `"이전 연도 archive 는 daily 갱신 대상이 아님"`
- 근거: CLAUDE.md §5.5 (경계 사례)
- 제안: 불변 구조 설명이므로 **유지 가능**. 다만 "작년/지난해" 같은 상대 표현은 지양. 현 표현("이전 연도"=지난 연도들)은 구조적 의미로 허용
- 우선순위: Low (경계 사례, 판단 필요)

### [HIST-04] "Stage N" 표현은 허용 (참고용 기록)
- 위치: `docs/DESIGN_QBT_LIVE_FINAL.md §4.3:147-155`
- 표현: `"Stage 1~7"`, `"Stage 3 직전"`
- 판정: `"Stage"` 는 알고리즘 실행 순서를 뜻하는 용어로, CLAUDE.md §5.5 의 금지 대상 "Phase(개발 단계)" 와 다름. **허용**
- 제안: 혼동 방지를 위해 CLAUDE.md §5.5 에 "Stage=알고리즘 순서 허용, Phase=개발 단계 금지" 구분 주석 추가
- 우선순위: Low (현 사용은 정당)

---

## 전체 요약

| 카테고리 | 항목 수 | High | Medium | Low |
|---|---:|---:|---:|---:|
| 1. 비즈니스 로직 오류 | 1 | 0 | 1 | 0 |
| 2. 버그 | 3 | 0 | 1 | 2 |
| 3. 상수화 필요 | 4 | 0 | 3 | 1 |
| 4. 리팩토링 | 5 | 0 | 3 | 2 |
| 5. 구조개선 | 4 | 1 | 0 | 3 |
| 6. 불필요한 fallback | 3 | 0 | 0 | 3 |
| 7. 문서/주석/코드 3자 불일치 | 1 | 0 | 0 | 1 |
| 8. 문서 내 쉽게 변경될 수치/리스트 | 2 | 0 | 1 | 1 |
| 9. 문서·주석 내 과거 상태/변경 이력/계획 단계 | 4 | 1 | 1 | 2 |
| **합계** | **27** | **2** | **10** | **15** |

## 우선 처리 권장 (High 항목)

1. **[ST-01]** services → store 의존 방향 위반 (auth.ts, fcm.ts, network.ts) — 아키텍처 정책 위반
2. ~~**[HIST-01]** DESIGN 문서 §8.2.4 "제거되었다" 과거 상태 기록~~ — **자동 해결** (서버 측 정리)

## 처리 진행 현황

| 계획서 | 범위 | 상태 |
|---|---|---|
| [PLAN_01](PLAN_01_drift_pct.md) | BL-01 (drift_pct 표시 + 배열 제거), HIST-01 (자동 해결) | **완료** |
| [PLAN_02](PLAN_02_code_cleanup.md) | BUG-01~03, FB-01~03 | **완료** |
| [PLAN_03](PLAN_03_constants.md) | CONST-01~03, RF-05 | **완료** |
| [PLAN_04](PLAN_04_refactor_struct.md) | ST-01~03, RF-01/03/04, CONST-04, DV-01, HIST-02 (ST-04, RF-02 는 YAGNI 보류) | **완료** |

## 다음 행동

- 본 문서는 **리스트업 전용**. 수정은 항목별로 사용자 승인 후 진행 (Phase 단위 커밋 원칙).
- 각 항목 처리 시 이 문서의 ID(예: `[ST-01]`) 를 커밋 메시지에 포함 권장.
- 처리 완료 항목은 취소선 또는 `완료` 태그로 갱신.

**문서 끝**
