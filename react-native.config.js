// Pretendard 폰트 링크용 설정(CLAUDE.md §0 — Android 전용이지만 RN 템플릿상 ios/ 는 존재).
// project 블록 생략 → CLI 기본값(플랫폼 자동 탐지) 사용. iOS autolink 는 실제 iOS 빌드 시에만 관여.
module.exports = {
  assets: ['./assets/fonts/'],
};
