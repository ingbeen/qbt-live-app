// Phase 9 폰트 스캐폴딩. iOS 는 지원 대상 아님(CLAUDE.md §0) → project.ios 비활성.
// 실제 Pretendard .ttf 파일 배치 + `npx react-native-asset` 실행은 후속 세션 이관.
module.exports = {
  project: { ios: null },
  assets: ['./assets/fonts/'],
};
